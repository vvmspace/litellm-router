[project]
name = "litellm-router-installer"
version = "1.0.0"
description = "A most gentlemanly installer for litellm-router"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
litellm-router-install = "installer:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
